apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Only references env overlays — ./base/headlamp is applied EXCLUSIVELY by
# the per-env Flux Kustomization (prod/headlamp/flux-kustomization.yaml).
resources:
  - ./prod
