apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The app's control-plane library (GitRepository + image automation), applied
# by the per-env Flux Kustomization (see prod/flux-kustomization.yaml), not
# aggregated directly. The workload manifests live in the external
# magmamoose/git-pull-request-dashboard repo.
resources:
  - ./git-pull-request-dashboard
