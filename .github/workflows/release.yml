name: Release

on:
  push:
    branches: [main]
    paths-ignore:
      - "**.md"
      - ".gitignore"
      - "CHANGELOG.md"
      - "LICENSE"
  workflow_dispatch:
    inputs:
      bump:
        description: Version bump type
        required: false
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major

concurrency:
  group: release-runner-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      # Release Runner public-app mode uses OIDC to request an installation token
      # for release writes; the workflow token itself only needs read access.
      contents: read # Release writes use the public-app installation token.
      id-token: write
    steps:
      - uses: magmamoose/diatreme@43efe38cb3515e19319f7f1173b14a28ae3b5dd4 # v2.1.0
        with:
          environment: prod
          environments: '["prod"]'
          prerelease-identifiers: "{}"
          force-bump: ${{ github.event_name == 'workflow_dispatch' && inputs.bump || '' }}
