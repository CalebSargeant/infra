apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: automation
spec:
  selector:
    app: openclaw
  ports:
    - name: http
      protocol: TCP
      port: 18789
      targetPort: 18789
  type: ClusterIP
