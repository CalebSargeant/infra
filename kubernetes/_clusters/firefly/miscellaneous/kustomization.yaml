apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: misc
  labels:
    app.kubernetes.io/sops: enabled
resources:
  - ../../../_base/miscellaneous/namespace.yaml
  # excalidraw, nextcloud, syncthing, wordpress, headlamp migrated to
  # kubernetes/apps/<app>/ — owned by Kustomization/flux-system/prod-<app>
