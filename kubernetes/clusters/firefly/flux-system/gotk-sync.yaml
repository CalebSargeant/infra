# This manifest was generated by flux. DO NOT EDIT.
# Phase E (Phase 3 of the restructure) — relocated from
# ./kubernetes/clusters/firefly/flux-system; the legacy `_clusters/`
# tree no longer exists. A `flux bootstrap` re-run would re-emit this
# file at the new path; the path edit below has the same effect.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/CalebSargeant/infra
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/clusters/firefly/flux-system
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-keys
