apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mikrotik-2-credentials
  namespace: core
spec:
  secretStoreRef:
    name: oci-vault
    kind: ClusterSecretStore
  target:
    name: mikrotik-2-credentials
    creationPolicy: Owner
  data:
    - secretKey: baseurl
      remoteRef:
        key: mikrotik-2-credentials
        property: baseurl
    - secretKey: username
      remoteRef:
        key: mikrotik-2-credentials
        property: username
    - secretKey: password
      remoteRef:
        key: mikrotik-2-credentials
        property: password
