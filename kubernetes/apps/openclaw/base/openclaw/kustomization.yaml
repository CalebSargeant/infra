apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
  - externalsecret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
components:
  # Migrated off ff-pi1: static hostPath pv.yaml dropped, pvc.yaml now Longhorn,
  # pinned to the worker node (k3s agent) instead of the Pi (system node).
  - ../../../../components/node-selectors/native-cloud
